# Semana 02 - Fundamentos de Clases, Relaciones y Colecciones

## 📝 Descripción

Esta entrega evoluciona el sistema de la inmobiliaria "Bienes Raíces Plus" añadiendo nuevas clases para modelar las transacciones y una clase gestora que usa colecciones. El foco principal es la implementación de **relaciones entre objetos** y el uso del contenedor **`ArrayList`**.

## 🎯 Objetivos Cumplidos

- [x] Implementación de 2 nuevas clases de negocio (`Cliente.java` y `Contrato.java`).
- [x] Implementación de la clase gestora (`Portafolio.java`).
- [x] Creación de **relaciones de agregación** (el `Contrato` contiene al `Cliente`, la `Propiedad` y el `Asesor`).
- [x] Uso de **`ArrayList`** en la clase `Portafolio` para manejar listas de objetos.
- [x] Código fuente completo y funcional (`Main.java`).

## 📂 Archivos Entregados

* `src/Propiedad.java`
* `src/AsesorComercial.java`
* `src/Cliente.java`
* `src/Contrato.java`
* `src/Portafolio.java`
* `src/Main.java`

## 🚀 Instrucciones de Ejecución

Para compilar y ejecutar el programa desde la terminal (asume que se está en la carpeta `semana-02/src/`):

```bash
javac *.java 
java Main